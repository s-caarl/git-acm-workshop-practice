//4 function integer calculator

#include <stdio.h>

int addition(x, y);
int subtraction(x, y);
int multiplication(x, y);
int division(x,y);

int main()
{
	return 0;
}

int addition(x, y)
{
	int sum = 0;
	
	sum = x + y;
	
	return sum;
}

int subtraction(x, y)
{
	int diff = 0;
	
	diff = x - y;

	return 0;
}

int multiplication(x, y)
{
	int prod = 0;
	
	prod = x* y;
	
	return prod;
}

int division(x,y)
{
	int quo = 0;
	
	quo = x / y;

	return 0;
}

