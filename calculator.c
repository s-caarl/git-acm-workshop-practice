//4 function integer calculator

#include <stdio.h>

int addition(x, y);
int subtraction(x, y);
int multiplication(x, y);
int division(x,y);

int main()
{
	return 0;
}

int addition(x, y)
{
	return 0;
}

int subtraction(x, y)
{
	return 0;
}

int multiplication(x, y)
{
	return 0;
}

int division(x,y)
{
	return 0;
}

